replicaCount: 1

image:
  repository: nginx
  tag: alpine
  pullPolicy: IfNotPresent

service:
  port: 8080
  targetPort: 80

route:
  enabled: false

probes:
  readiness:
    path: /
  liveness:
    path: /

resources: {}